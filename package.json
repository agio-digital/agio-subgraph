{
  "name": "agio-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ agio-digital/agio-subgraph",
    "create-local": "graph create --node http://localhost:8020/ agio-digital/agio-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ agio-digital/agio-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 agio-digital/agio-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.0",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
